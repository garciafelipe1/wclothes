# URL del backend Medusa (Store API)
NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9001

# Publishable API key (Store API). Obtenerla en el dashboard del backend: Settings → Publishable API Keys
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=
